{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn neon_proxy:app",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}